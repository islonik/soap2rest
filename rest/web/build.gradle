dependencies {
    compile project(':rest:impl')

    compile "org.springframework.boot:spring-boot-starter-security:${versions.springBoot}"
    compile "org.springframework.boot:spring-boot-starter-aop:${versions.springBoot}"
}
